// Copie este arquivo para `supabase-config.js` e preencha com as suas credenciais.
// NUNCA commitar `supabase-config.js` com dados reais (ele já está no .gitignore).
//
// A senha do admin é gerenciada pelo Supabase Auth — não fica neste arquivo.
// Crie o usuário admin em: Supabase Dashboard → Authentication → Users → Add user.
window.SUPABASE_CONFIG = {
  url: "https://COLE_A_URL_DO_SEU_PROJETO.supabase.co",
  anonKey: "COLE_A_ANON_KEY_DO_SEU_PROJETO",
  officialMatchesUrl: "https://raw.githubusercontent.com/openfootball/worldcup.json/master/2026/worldcup.json"
};
